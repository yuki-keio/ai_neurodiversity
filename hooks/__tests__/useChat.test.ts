import { describe, it, expect } from 'vitest';

describe('useChat', () => {
	it('should return initial state', () => {
		const initialState = {}; // 初期状態を定義
		expect(initialState).toEqual({});
	});
});